{
    "geode": "5.8.2",
    "gd": {
        "win": "2.2081",
        "android": "2.2081"
    },
    "version": "v0.16.0",
    "id": "com.yourname.coopedit",
    "name": "Coop Level Editor",
    "developer": "yourname",
    "description": "Edit levels together in real time",
    "settings": {
        "server-url": {
            "name": "Server Address",
            "description": "Leave this alone unless the server has moved. The room window uses this automatically.",
            "type": "string",
            "default": "wss://coop-level-editer.onrender.com"
        },
        "room-name": {
            "name": "Last Room",
            "description": "Remembered from the room window so you return to the same room. Empty means no room.",
            "type": "string",
            "default": ""
        },
        "show-cursors": {
            "name": "Show Partner Cursors",
            "description": "Show where your partner is touching the editor",
            "type": "bool",
            "default": true
        },
        "show-notifications": {
            "name": "Join Notifications",
            "description": "Show a message when someone joins or leaves your room",
            "type": "bool",
            "default": true
        },
        "auto-join": {
            "name": "Auto Connect",
            "description": "Connect to the server when you open the editor. You still pick the room yourself.",
            "type": "bool",
            "default": true
        },
        "tiny-hud": {
            "name": "Tiny Editor Bar",
            "description": "Show only a small COOP dot at the top of the editor instead of the status text. The room window still opens from it.",
            "type": "bool",
            "default": false
        },
        "hud-spot": {
            "name": "Editor Bar Spot",
            "description": "Where the small COOP bar sits. top-center overlaps the level scroll bar, so it is not the default.",
            "type": "string",
            "default": "top-left",
            "one-of": [
                "top-left",
                "top-right",
                "bottom-left",
                "bottom-right",
                "top-center"
            ]
        }
    }
}